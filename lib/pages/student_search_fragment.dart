import 'package:flutter/material.dart';

class StudentSearchFragment extends StatelessWidget {
  const StudentSearchFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Student Search Fragment'),
    );
  }
}
